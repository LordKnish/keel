# Database (Vercel Postgres / Neon)
POSTGRES_URL=
POSTGRES_PRISMA_URL=
POSTGRES_URL_NO_SSL=
POSTGRES_URL_NON_POOLING=
POSTGRES_USER=
POSTGRES_HOST=
POSTGRES_PASSWORD=
POSTGRES_DATABASE=

# Cron Job Security
# Generate a random secret: openssl rand -hex 32
CRON_SECRET=

# Background Removal API (Optional)
# Get a free API key at https://www.remove.bg/api
# Without this, silhouettes use edge detection only
REMOVEBG_API_KEY=
